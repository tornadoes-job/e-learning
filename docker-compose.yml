services:
  deps:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app
      - root_node_modules:/app/node_modules
    command: npm ci

  backend:
    image: node:20-alpine
    working_dir: /app
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: development
      PORT: 3000
      FRONTEND_URL: http://localhost:4200
      REDIS_URL: redis://redis:6379
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - root_node_modules:/app/node_modules
    command: npm run start:dev --workspace=backend
    depends_on:
      deps:
        condition: service_completed_successfully
      redis:
        condition: service_started
    restart: unless-stopped

  frontend:
    image: node:20-alpine
    working_dir: /app
    environment:
      VITE_API_URL: http://localhost:3000/api
    ports:
      - "4200:4200"
    volumes:
      - ./:/app
      - root_node_modules:/app/node_modules
    command: npm run start --workspace=frontend -- --host 0.0.0.0 --port 4200
    depends_on:
      deps:
        condition: service_completed_successfully
      backend:
        condition: service_started
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: xamxam-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: xamxam-grafana
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    depends_on:
      - prometheus
    restart: unless-stopped

  node-exporter:
    image: prom/node-exporter:latest
    container_name: xamxam-node-exporter
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--path.rootfs=/rootfs"
    restart: unless-stopped

volumes:
  root_node_modules:
  redis_data:
  prometheus_data:
  grafana_data:
